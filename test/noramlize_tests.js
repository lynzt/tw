'use strict';
const chai = require('chai');
const expect = chai.expect;

const normalize = require('./../lib/normalize');

describe('test helper functions in normalize', function() {
  describe('test stub', function() {
    // TODO: mock these tests...
    it('should return normalized line', function() {
      expect(true).to.be.true;
    });
    it('should return normalized file', function() {
      expect(true).to.be.true;
    });
  });

});
